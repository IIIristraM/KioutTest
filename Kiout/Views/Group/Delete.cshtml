@model Kiout.Models.Group

@{
    ViewBag.Title = "Удалить учебную группу";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>
<div>
    @Html.ActionLink("Учебные группы", "Index") > @ViewBag.Title
</div>
<br /><br />
<h3>Уверены, что хотите удалить группу?</h3>
<div>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Instructor.FullName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Instructor.FullName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Сourse.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Сourse.Title)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Удалить" class="btn btn-default" />
        </div>
    }
</div>
